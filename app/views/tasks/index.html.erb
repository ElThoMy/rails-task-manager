<h1>My Tasks 🧨</h1>
<% @tasks.each do |task| %>
  <h2><%= link_to task.title, task_path(task) %> -
  <%= link_to "❌", task_path(task), method: :delete, data: { confirm: "Are you sure you want to delete #{task.title} forever?" } %></h2>
<% end %>

<%= link_to "Create a new task", new_task_path %>
